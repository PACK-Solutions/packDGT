ktor:
  deployment:
    port: 8080
    port: ${PORT}

app:
  templates:
    directory: templates
  output:
    directory: output
    saveToDisc: false
  libreoffice:
    poolSize: 4          # double the throughput
    taskTimeoutMs: 600000 # 10 min if you really push it
